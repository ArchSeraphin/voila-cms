{% extends 'layouts/admin.html.twig' %}
{% block title %}Témoignages — {{ app.name }}{% endblock %}
{% block content %}
<div class="flex items-center justify-between mb-6">
    <h1 class="font-display text-2xl font-semibold">Témoignages</h1>
    <a href="/admin/temoignages/new" class="px-4 py-2 bg-primary text-white rounded hover:bg-blue-700 font-medium text-sm">+ Nouveau témoignage</a>
</div>
{% if rows|length == 0 %}
<div class="rounded-lg bg-white border border-slate-200 p-8 text-center text-slate-500">Aucun témoignage.</div>
{% else %}
<div class="rounded-lg bg-white border border-slate-200 overflow-hidden">
    <table class="w-full text-sm">
        <thead class="bg-slate-50 border-b border-slate-200">
            <tr>
                <th class="px-4 py-3 text-left font-medium">Photo</th>
                <th class="px-4 py-3 text-left font-medium">Auteur</th>
                <th class="px-4 py-3 text-left font-medium">Entreprise</th>
                <th class="px-4 py-3 text-left font-medium">Note</th>
                <th class="px-4 py-3 text-left font-medium">Ordre</th>
                <th class="px-4 py-3 text-left font-medium">Statut</th>
                <th class="px-4 py-3 text-right font-medium">Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for r in rows %}
            <tr class="border-b border-slate-100 last:border-0">
                <td class="px-4 py-3">{% if r.photo %}<img src="/{{ r.photo }}" class="h-10 w-10 rounded-full object-cover" alt="">{% endif %}</td>
                <td class="px-4 py-3 font-medium">{{ r.auteur }}</td>
                <td class="px-4 py-3 text-slate-600">{{ r.entreprise }}</td>
                <td class="px-4 py-3 text-yellow-500">{% if r.note %}{% for i in 1..5 %}{% if i <= r.note %}★{% else %}☆{% endif %}{% endfor %}{% endif %}</td>
                <td class="px-4 py-3 text-slate-600">{{ r.ordre }}</td>
                <td class="px-4 py-3">{% if r.published %}<span class="inline-block px-2 py-0.5 text-xs bg-green-100 text-green-800 rounded">Publié</span>{% else %}<span class="inline-block px-2 py-0.5 text-xs bg-slate-100 text-slate-600 rounded">Caché</span>{% endif %}</td>
                <td class="px-4 py-3 text-right space-x-3">
                    <a href="/admin/temoignages/{{ r.id }}/edit" class="text-primary hover:underline">Éditer</a>
                    <form method="post" action="/admin/temoignages/{{ r.id }}/delete" class="inline" onsubmit="return confirm('Supprimer ?');">
                        <input type="hidden" name="_csrf" value="{{ csrf() }}">
                        <button type="submit" class="text-red-600 hover:underline">Supprimer</button>
                    </form>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endif %}
{% endblock %}
